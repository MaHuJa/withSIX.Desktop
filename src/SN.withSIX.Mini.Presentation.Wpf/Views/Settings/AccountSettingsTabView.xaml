<UserControl x:Class="SN.withSIX.Mini.Presentation.Wpf.Views.Settings.AccountSettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settings="clr-namespace:SN.withSIX.Mini.Applications.ViewModels.Settings;assembly=SN.withSIX.Mini.Applications"
             xmlns:applications="clr-namespace:SN.withSIX.Core.Applications;assembly=SN.withSIX.Core.Applications"
             mc:Ignorable="d"
             d:DesignHeight="{StaticResource MainWindowMinHeight}" d:DesignWidth="{StaticResource MainWindowMinWidth}"
             d:DataContext="{d:DesignInstance settings:AccountSettingsTabViewModel}">
    <Grid>
        <StackPanel>
            <TextBlock x:Name="UserName" />
            <Button x:Name="Login" Content="Login" />
            <Button x:Name="Logout" Content="Logout" />
            <Button x:Name="GoPremium">
                <TextBlock>
                    <Run Text="{x:Static applications:SixIconFont.withSIX_icon_Badge_Sponsor}"
                         FontFamily="{StaticResource IconFont}" />
                    <Run x:Name="GoPremiumRun" Text="Go premium" />
                </TextBlock>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>