<UserControl x:Class="SN.withSIX.Mini.Presentation.Wpf.Views.Main.Games.Recent.RecentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:applications="clr-namespace:SN.withSIX.Core.Applications;assembly=SN.withSIX.Core.Applications"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="RecentList" Grid.Row="0" Style="{StaticResource ListBoxStyle}" />

        <Button Style="{StaticResource MetroTextButtonStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                x:Name="AddSomeContent" FontSize="{StaticResource HintFontSize}">
            <TextBlock>
                <Run Text="{x:Static applications:SixIconFont.withSIX_icon_Nav_Server}" FontFamily="{StaticResource IconFont}" />
                <Run Text="Use some content" />
            </TextBlock>
        </Button>
    </Grid>
</UserControl>