<UserControl x:Class="SN.withSIX.Mini.Presentation.Wpf.Views.Main.Games.Favorite.FavoriteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SN.withSIX.Mini.Presentation.Wpf.Views.Main.Games.Favorite"
             xmlns:applications="clr-namespace:SN.withSIX.Core.Applications;assembly=SN.withSIX.Core.Applications"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="FavoriteItems" Grid.Row="0" Style="{StaticResource ListBoxStyle}" />
        <TextBlock x:Name="AddFavoritesText" HorizontalAlignment="Center"
                   VerticalAlignment="Center" FontSize="{StaticResource HintFontSize}">
            <Run Text="{x:Static applications:SixIconFont.withSIX_icon_Star}" FontFamily="{StaticResource IconFont}" />
            <Run Text="Star some items" />
        </TextBlock>
    </Grid>
</UserControl>