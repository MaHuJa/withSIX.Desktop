<UserControl x:Class="SN.withSIX.Play.Presentation.Wpf.Views.Dialogs.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Width="800" Height="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="Warning" Grid.Row="0" />
        <WebBrowser x:Name="Browser" Grid.Row="1" />
        <Button Content="Cancel" Command="{Binding Close}" Grid.Row="2" />
    </Grid>
</UserControl>